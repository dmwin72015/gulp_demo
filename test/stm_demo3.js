'usr strict';
const Readable = require('stream').Readable;
// Generator 函数 ES6  
// TODO: 学习
class dmReadable extends Readable {

  constructor(iterable) {
    super()
    this.iterable = new funtion * (argument) {
      yield * iterable
    }
  }
}
